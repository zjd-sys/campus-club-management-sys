# ===== 管理前端环境变量示例 =====
# 复制为 .env.development / .env.production 或 .env.local 后按需修改。
# 所有项均为「可选」：不配置时使用相对路径 + 开发代理，开箱即用。

# 管理端接口基址。缺省 /api/admin（同源相对路径）
VITE_ADMIN_API_BASE_URL=/api/admin

# 门户端接口基址（管理端少数接口会调用门户命名空间，如社团成员）
VITE_API_BASE_URL=/api/portal

# 静态文件基址。留空表示同源相对路径；CDN/对象存储时填写完整前缀
VITE_FILE_BASE_URL=

# —— 仅开发环境（vite dev server）生效 ——
VITE_DEV_PROXY_TARGET=http://localhost:8080
VITE_DEV_PORT=5174
